# Development image: keeps devDependencies (tsx, typescript) for hot-reload.
# Used by docker-compose.yml. For production, use ./Dockerfile instead.
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

EXPOSE 3000

CMD ["npm", "run", "dev"]
